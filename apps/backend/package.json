{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "[ -d venv ] || (python3 -m venv venv && ./venv/bin/pip install -r requirements.txt); ./venv/bin/uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start:win": "if not exist venv (python -m venv venv && .\\venv\\Scripts\\pip install -r requirements.txt) && .\\venv\\Scripts\\uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "seed": "./venv/bin/python seed.py",
    "seed:win": ".\\venv\\Scripts\\python seed.py"
  }
}
