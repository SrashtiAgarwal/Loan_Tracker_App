# packages/

This directory is reserved for shared packages in the Turborepo monorepo.

## Examples of packages you might add:

- `packages/types/` — Shared TypeScript types/interfaces between frontend and backend codegen
- `packages/config/` — Shared ESLint, TypeScript base configs
- `packages/ui/` — Shared React Native UI components
